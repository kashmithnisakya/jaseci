- **WebSocket Transport**: `WebSocketTransport` now drives the underlying FastAPI socket (accept/send/close) instead of the previous placeholder, and all transports share a single `serialize_transport_response()` envelope helper. Added a `streaming: bool` flag to `RESTSpec` so endpoints can opt into streamed responses.
