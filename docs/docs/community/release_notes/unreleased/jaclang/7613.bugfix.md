- **Scale: embedder-supplied `microservices_config` now receives schema defaults**: the manifest builder normalizes a caller-provided config through the same defaulting path the file config uses, so defaulted keys like `client.entry` survive and embedded deploys no longer ship a headless fleet silently; caller-set values still win, and `client.entry = false` still disables the client build explicitly.
