- **Static `@schedule` tasks no longer log a phantom `Invalid walker object` error every tick**: `register_static_tasks` registered its self-executing closure with `is_walker=True`, so the base scheduler ran the job and then also tried to `spawn` the closure's `None` return value. The closure now registers as a plain function and base `Scheduler._execute` verifies the target produced a walker before spawning.
