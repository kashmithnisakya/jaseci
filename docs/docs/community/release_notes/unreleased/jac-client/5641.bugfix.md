- **Diagnostics Parser: Structured Error Format and Help Text Support**: The compilation error detector now correctly extracts error messages and source locations from the structured `error[CODE]: message` format in addition to the legacy `line X, col Y: message` format. Help text appearing on subsequent lines no longer pollutes the extracted error message shown in the IDE.
